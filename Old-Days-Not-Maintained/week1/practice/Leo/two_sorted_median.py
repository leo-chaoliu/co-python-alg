# Assume that you are given two sorted arrays A and B, each of them 
# containing n numbers. Give an efficient algorithm for computing one of the two 
# medians of all 2n numbers and analyze its running time
def two_sorted_median(array1, array2):
    # len(array1) = len(array2)
    n = len(array1)
    mid1 = mid2 = n//2

    



two_sorted_median([3,5,7],[4,7,9])